// Simplified bootstrap - minimal worker initialization only
export async function bootstrapWorkers(): Promise<void> {
  console.log('[BOOT] Optimized worker initialization - minimal setup only');
  // No complex bootstrap needed for optimized version
}
