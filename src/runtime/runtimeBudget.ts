/**
 * Purpose: backward-compatible runtime budget export surface.
 * Inputs/Outputs: re-exports platform resilience runtime budget helpers/types.
 * Edge cases: supports legacy imports while platform modules are canonical.
 */
export * from '@platform/resilience/runtimeBudget.js';
