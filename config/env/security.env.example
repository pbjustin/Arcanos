# ================================
# DAEMON / CLI RATE LIMITS
# ================================
# Limits for CLI agent (heartbeat, commands, registry, update). Per-IP, per window.
# Max requests per window (default: 400 for daemon, 120 for registry)
# DAEMON_RATE_LIMIT_MAX=400
# DAEMON_REGISTRY_RATE_LIMIT_MAX=120
# Window length in ms (default: 10 minutes)
# DAEMON_RATE_LIMIT_WINDOW_MS=600000
# DAEMON_REGISTRY_RATE_LIMIT_WINDOW_MS=600000

# ================================
# SECURITY & ADMINISTRATION
# ================================

# Admin Access Configuration
# Admin key for /admin endpoints and privileged operations
ADMIN_KEY=your-admin-key-here

# Root Override Configuration (Development Only)
# Allow root-level access bypassing normal security checks
ALLOW_ROOT_OVERRIDE=true
ROOT_OVERRIDE_TOKEN=REDACTED_ROOT_OVERRIDE_TOKEN

# ARCANOS API Token (for /api/memory endpoints)
# Internal API authentication token
ARCANOS_API_TOKEN=your-arcanos-api-token-here

# GPT API Access Token (for diagnostics endpoint)
# Used for system diagnostics and health monitoring
GPT_TOKEN=your-gpt-access-token-here

# Automation webhook secret/header for bridge socket integrations
# ARCANOS_AUTOMATION_SECRET=your-automation-secret
# ARCANOS_AUTOMATION_HEADER=x-arcanos-automation
