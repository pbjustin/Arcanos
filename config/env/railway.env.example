# ================================
# RAILWAY DEPLOYMENT
# ================================
# Note: Railway automatically injects the following variables:
# - PORT: Server port (required)
# - RAILWAY_ENVIRONMENT: Environment identifier (production, development, pr-*)
# - DATABASE_URL: PostgreSQL connection string (if database service attached)
# These do not need to be set manually in Railway dashboard.

# Railway Project Configuration
RAILWAY_PROJECT=arcanos-core

# Railway Project ID (auto-detected on Railway platform)
# RAILWAY_PROJECT_ID=

# Railway Service ID (auto-detected on Railway platform)
# RAILWAY_SERVICE_ID=

# Railway Environment (auto-detected on Railway platform)
# Values: production | development | pr-* (for preview deployments)
RAILWAY_ENVIRONMENT=production

# Railway Deployment ID (auto-detected during deployment)
# RAILWAY_DEPLOYMENT_ID=

# Railway Release ID (auto-detected during deployment)
# RAILWAY_RELEASE_ID=

# Railway Management API Token (enables deploy/rollback automation)
# Generate from: https://railway.app/account/tokens
RAILWAY_API_TOKEN=

# Railway OpenAI API Key Override (optional, overrides OPENAI_API_KEY when set)
# RAILWAY_OPENAI_API_KEY=

# Railway OpenAI Model Override (optional, overrides AI_MODEL when set)
# RAILWAY_OPENAI_MODEL=

# Railway OpenAI Fallback Model Override (optional)
# RAILWAY_OPENAI_FALLBACK_MODEL=

# Railway GraphQL Endpoint (for Railway API integrations)
# Default: https://backboard.railway.app/graphql/v2
# RAILWAY_GRAPHQL_ENDPOINT=https://backboard.railway.app/graphql/v2

# Railway GraphQL Timeout (milliseconds)
# Default: 15000ms (15 seconds). Override to match your Railway plan limits.
# RAILWAY_GRAPHQL_TIMEOUT_MS=15000

# Railway Validation Patterns (comma-separated list of validation patterns)
# Used for deployment validation and health checks
# RAILWAY_VALIDATION_PATTERNS=

# Public API URL (set by Railway, can be overridden)
API_URL=https://arcanos-v2-production.up.railway.app

# API Route Configuration (Railway Compatibility)
MODEL_ROUTE=/query-finetune
LOGIC_ROUTE=/ask
# External registry endpoint for dual-mode audit verification
BACKEND_REGISTRY_URL=https://your-backend-service.com/registry

# Optional context snapshot tag for orchestration bootstrapping
# CONTEXT_SNAPSHOT_TAG=orchestration

# Optional orchestration reset marker
# ORCHESTRATION_LAST_RESET=
