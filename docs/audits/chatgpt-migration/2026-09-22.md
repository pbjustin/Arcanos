# Tutor migration foundation verification

Historical evidence from 2026-09-22, against an isolated feature worktree based
on freshly fetched `origin/main`
`655cb56fc3912684a0dd7b7bfb735a841f3c4056`. No overlapping open/draft PR was
found. The draft PR records the final head SHA; these commands exercised its
source changes before publication. The unrelated dirty checkout was preserved.

Toolchain: Node `24.18.1`, bundled npm `11.16.0`. Dependencies installed with
`CI=true npm ci --no-audit --no-fund`; no dependency or lockfile upgrade.
No production credential, issuer, persistence endpoint or worker was exercised.

## Boundary evidence

Final combined run: **25 suites / 391 tests passed**. All five new suites
(161 tests) were also executed independently by the reviewer.

| Check | Result and precise boundary |
| --- | --- |
| Actual JWT verifier | PASS: 103 signed-token cases, RS256/ES256, wrong signature/issuer/audience/type/time/scopes, legacy credential collision, immutable identity and expiry recheck. Local key fixtures and loopback JWKS retrieval/cache/redirect/timeout. |
| Assembled new route | PASS: 31 cases using the real MCP SDK initialize, initialized notification, tools/list and tools/call. Includes strict input, direct unadvertised calls, metadata/challenges, principal isolation, unsafe runtime, shared admission exhaustion/failure, batch rejection, timeout and actual HTTP disconnect. Generation is a dependency fixture in this suite. |
| Real application composition | PASS: 6 cases, real createApp and legacy authentication/SDK transport; provider/catalog/dispatch/startup fixtures. Disabled/misconfigured new integration preserves existing routes. Tutor query still fails closed without the required queue database; ping reaches its fixture. |
| Package source | PASS: 13 cases including official portable schema, allowlisted contents, unsafe paths/symlinks and credential exclusion. Release remains BLOCKED; no archive is emitted. |
| Real Tutor pipeline | PASS: 8 cases, actual signed identity through Tutor, Trinity response mapping and HRC, with synthetic provider/storage sinks. Observes no memory access or optional writes, provider store:false, cleared inherited history, principal isolation, shortcut/mock labels and cancellation/error behavior. |
| Existing related regression | PASS: 15 suites / 173 tests, commands below. Includes real GPT/session-to-synthetic-provider tests; storage/AI remain isolated fixtures. |
| Protocol families | PASS: 4 suites / 55 tests. No daemon command or Python capability is added. |
| Shared provider app composition | PASS: 2 tests, existing middleware composition and admission behavior. |

The route tests' synthetic legacy handler proves passthrough only; the separate
createApp and existing GPT/MCP suites provide the compatibility evidence.
No component test is a real ChatGPT E2E test. MCP cancellation notifications
on another POST are not correlated by this stateless pilot; actual transport
disconnect and aggregate timeout cancellation are the verified mechanisms.

## Exact validation commands

Run from repository root with the pinned toolchain:

```text
npm run type-check
npm run lint
npm run build
npm run validate:backend-cli:contract
npm run validate:backend-cli:offline
npm run validate:railway
npm run reindex
npm run docs:check
npm run docs:links -- --local-only
npm run sync:check
npm run guard:commit
git diff --check
node scripts/run-jest.mjs --runTestsByPath tests/chatgpt-auth.test.ts tests/chatgpt-mcp.integration.test.ts tests/chatgpt-app-composition.test.ts tests/chatgpt-tutor.pipeline.test.ts tests/tutor-pilot-package.test.ts --coverage=false --runInBand
node scripts/run-jest.mjs --runTestsByPath tests/tutor-logic.prompt-forwarding.test.ts tests/mcp-route-isolation.test.ts tests/mcp-http-body-cap.test.ts tests/mcp-http-global-json-limit.test.ts tests/mcp-context.test.ts tests/mcp-error-disclosure.test.ts tests/mcp-job-tools.test.ts tests/mcp-control-plane-tools.test.ts tests/gpt-session-context.e2e.test.ts tests/gpt-session-context.integration.test.ts tests/gpt-dispatch.mcp.test.ts tests/hrc.test.ts tests/hrcWrapper.memoryInspection.test.ts tests/trinity-direct-answer-stage.test.ts tests/trinity-direct-answer-mode.test.ts --coverage=false
node scripts/run-jest.mjs --runTestsByPath tests/action-plan-execution-protocol-contract.test.ts tests/backstage-booker-protocol-contract.test.ts tests/control-plane-protocol.test.ts tests/local-agent-http-protocol.test.ts --coverage=false
node scripts/run-jest.mjs --runTestsByPath tests/public-provider-admission-app-composition.test.ts --coverage=false --runInBand
node scripts/validate-tutor-pilot-package.mjs
node scripts/validate-tutor-pilot-package.mjs --release
```

Static checks passed. Lint reports 0 errors and 76 existing warnings; focused
changed-file lint reports none. Sync reports 0 errors, 0 warnings and 5
advisory information items. Package `--release` returns exit 2 intentionally:
the real registered connection, account eligibility and reconciliation evidence
are missing. This is a tested release block, not a successful install.

## Review and deployment limits

Actual agents separately handled architecture/inventory, authentication,
packaging/platform research and read-only combined-diff verification. The
independent reviewer found early-route bypasses of runtime safety and the
shared provider budget. Both were repaired with execution checks, shared
admission reuse and batch rejection, then independently retested. The review
also identified the cross-POST cancellation limitation documented above.
Final independent verification passed all five new suites (161 tests) and found
no remaining blocker in the combined runtime/package diff. This is agent review,
not a human approval or permission to merge.

The inspected lifecycle requires non-draft, same-repository, main-targeted PRs
with `railway-preview` (controller lines 492-497). This task requires a draft.
No preview was created or labeled. The sealed preview app does not serve this
normal OAuth route, so an unrelated preview health result would not validate it.
Production promotion only follows the separately held main workflow; no
production settings, credentials, deployments or rollout holds were changed.

| Evidence dimension | Status |
| --- | --- |
| Repository implementation and local component tests | IMPLEMENTED / TESTED |
| Deployed OAuth resource/transport at exact head | NOT VERIFIED; no eligible isolated target exercised |
| Real identity-provider account authorization | BLOCKED; approved provider/tenant/client policy required |
| Registered or installed plugin | BLOCKED; real connection reference required; release withheld |
| ChatGPT web | NOT VERIFIED; account/workspace eligibility and connection pending |
| ChatGPT iOS | BLOCKED / NOT VERIFIED; current custom MCP help says web only |
| Desktop/Codex installed behavior | NOT VERIFIED; package schema validation is not installation |
| Published Builder and uploaded reference parity | BLOCKED; private exports/inventory not available |
| Production persistence, memory, canon or source tests | NOT RUN; outside authorization |
| Full repository Jest, live PostgreSQL/provider quality and physical-device tests | NOT RUN locally; focused suites only |

Remaining setup and follow-up scopes are in the
[inventory](../../chatgpt-migration/INVENTORY.md) and
[provider contract](../../chatgpt-migration/AUTHENTICATION.md). Rollback needs
only the authorized target's enable flag disabled and application restart;
there is no database or legacy-route prerequisite.
