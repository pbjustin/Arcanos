## Summary
Addresses PR analysis report items: dead/bloated code, simplification, and Railway readiness.

### Changes
- **Trinity helpers (Option B):** Added `src/logic/trinityHelpers.ts` with `calculateMemoryScoreSummary` and `logFallbackEvent`; `trinityStages.ts` imports from it and re-exports `calculateMemoryScoreSummary`.
- **Constants:** Centralized `FALLBACK_PROMPT_SNIPPET_LENGTH` in `constants.ts`; `fallbackMessages.ts` and `prompts.ts` use it.
- **Model literals:** `tokenParameterHelper.ts` and `requestTransforms.ts` use `APPLICATION_CONSTANTS.MODEL_GPT_5` / `MODEL_GPT_5_1`; `credentialProvider.ts` uses `MODEL_GPT_4` / `MODEL_GPT_4O` for fallback/complex defaults.

### Report items
1. Dead/Bloated - extract utilities: **Done** (trinityHelpers)
2. Simplification - numeric constants: **Done** (FALLBACK_PROMPT_SNIPPET_LENGTH)
3. OpenAI SDK: No changes (confirmed OK)
4. Railway - hardcoded values: **Done** (model literals via APPLICATION_CONSTANTS)
