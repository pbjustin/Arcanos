# ================================
# ARCANOS BACKEND CONFIGURATION
# ================================
# This index file contains the minimum required settings.
# For full configuration options, copy sections from:
#   - config/env/core.env.example
#   - config/env/openai.env.example
#   - config/env/railway.env.example
#   - config/env/integrations.env.example
#   - config/env/security.env.example
#   - config/env/advanced.env.example
#   - config/env/development.env.example

# ================================
# CORE SYSTEM CONFIGURATION (MINIMUM)
# ================================

# Node.js Runtime Environment
# Values: development | production | test
NODE_ENV=development

# Server Port Configuration
# Default: 3000 when unset (see APPLICATION_CONSTANTS.DEFAULT_PORT in code)
# Railway: Automatically set by Railway via $PORT environment variable
PORT=3000

# Server Host Binding
# Default: 127.0.0.1 in development, 0.0.0.0 in production
# Development binds to localhost for security. For network access (Docker, WSL2, remote testing):
# HOST=0.0.0.0
# HOST=127.0.0.1

# Optional base URL for reverse proxies or custom gateways
# SERVER_URL=http://127.0.0.1:3000

# Log and memory storage paths (Railway uses ephemeral /tmp)
# ARC_LOG_PATH=/tmp/arc/log
# ARC_MEMORY_PATH=/tmp/arc/memory

# OpenAI request timeout in ms (applies to worker API calls)
# WORKER_API_TIMEOUT_MS=30000

# Server Host Binding (Optional)
# Development default: 127.0.0.1 (localhost only - more secure)
# Production default: 0.0.0.0 (all interfaces)
# Set HOST=0.0.0.0 in development if you need network access
# See config/env/development.env.example for details
# HOST=0.0.0.0

# ================================
# OPENAI INTEGRATION (REQUIRED)
# ================================

# OpenAI API Key - REQUIRED for all AI functionality
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Primary AI Model Configuration
# Latest stable alternatives: gpt-4o (recommended), gpt-4o-mini (cost-effective)
OPENAI_MODEL=gpt-4o-mini

# Optional OpenAI base URL override
# OPENAI_BASE_URL=https://api.openai.com/v1

# Retry policy for transient OpenAI errors
# OPENAI_MAX_RETRIES=2
