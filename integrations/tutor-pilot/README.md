# Tutor package moved

The standalone migration candidate is now
[ARCANOS TUTOR](../arcanos-tutor/README.md). This directory retains only the
historical link target; it is not a second package.

The old validator command remains a compatibility entry point for the new
validator. Historical PR #1508 audit reports describe the old staging layout.
