{
  "name": "backend-cli-contract",
  "contractVersion": "1.0.0",
  "schemaVersion": 1,
  "description": "Versioned interface between TypeScript backend routes and Python daemon backend client.",
  "operations": {
    "request_query": {
      "endpoint": "/gpt/{gptId}",
      "method": "POST",
      "plane": "writing",
      "canonicalAction": "query",
      "preferredClientSurface": "Async GPT bridge create-job operation. Use this when the caller needs a durable job id without waiting for final output."
    },
    "request_query_and_wait": {
      "endpoint": "/gpt/{gptId}",
      "method": "POST",
      "plane": "writing",
      "canonicalAction": "query_and_wait",
      "preferredClientSurface": "Async GPT bridge create-and-wait operation. Use this when the caller can wait briefly for fast completion but still needs the same durable job id on timeout."
    },
    "request_gpt_job_status": {
      "endpoint": "/jobs/{id}",
      "method": "GET",
      "plane": "control",
      "canonicalAction": "get_status",
      "requiredHeader": "x-arcanos-job-read-token",
      "preferredClientSurface": "Compatibility method name over the direct canonical job-status endpoint. Supply the job-specific `jobReadToken` returned by creation, or the transient `ARCANOS_JOB_READ_TOKEN` client environment value. Never route job-status reads through /gpt/{gptId}."
    },
    "request_gpt_job_result": {
      "endpoint": "/jobs/{id}/result",
      "method": "GET",
      "plane": "control",
      "canonicalAction": "get_result",
      "requiredHeader": "x-arcanos-job-read-token",
      "preferredClientSurface": "Compatibility method name over the direct canonical job-result endpoint. Supply the job-specific `jobReadToken` returned by creation, or the transient `ARCANOS_JOB_READ_TOKEN` client environment value. Never route job-result reads through /gpt/{gptId}."
    },
    "request_job_status": {
      "endpoint": "/jobs/{id}",
      "method": "GET",
      "plane": "control",
      "canonicalAction": "get_status",
      "requiredHeader": "x-arcanos-job-read-token",
      "preferredClientSurface": "Direct canonical capability-bound job-status endpoint for agent and tool clients."
    },
    "request_job_result": {
      "endpoint": "/jobs/{id}/result",
      "method": "GET",
      "plane": "control",
      "canonicalAction": "get_result",
      "requiredHeader": "x-arcanos-job-read-token",
      "preferredClientSurface": "Direct canonical capability-bound job-result endpoint for agent and tool clients."
    },
    "request_system_state": {
      "endpoint": "/system-state",
      "method": "POST",
      "plane": "control",
      "canonicalAction": "system_state",
      "preferredClientSurface": "Direct system-state endpoint. Never route system-state reads or writes through /gpt/{gptId}."
    }
  },
  "endpoints": {
    "/gpt/{gptId}": {
      "method": "POST",
      "tsRouteFile": "src/routes/gptRouter.ts",
      "pythonClientMethods": [
        "request_ask_with_domain",
        "request_chat_completion",
        "request_query",
        "request_query_and_wait"
      ],
      "requestFields": [
        "action",
        "context",
        "executionMode",
        "gptId(path)",
        "messages",
        "mode",
        "pollIntervalMs",
        "patch",
        "prompt",
        "expectedVersion",
        "timeoutMs",
        "waitForResultMs",
        "payload"
      ],
      "asyncResponseFields": [
        "jobId",
        "jobReadToken",
        "jobReadTokenHeader",
        "poll",
        "stream"
      ]
    },
    "/system-state": {
      "method": "POST",
      "tsRouteFile": "src/routes/system-state.ts",
      "pythonClientMethods": [
        "request_system_state"
      ],
      "requestFields": [
        "metadata",
        "sessionId",
        "context",
        "expectedVersion",
        "patch"
      ]
    },
    "/jobs/{id}": {
      "method": "GET",
      "tsRouteFile": "src/routes/genericJobsRouter.ts",
      "pythonClientMethods": [
        "request_gpt_job_status",
        "request_job_status"
      ],
      "requestFields": [
        "id(path)",
        "jobReadToken(header:x-arcanos-job-read-token)"
      ],
      "requiredHeaders": [
        "x-arcanos-job-read-token"
      ],
      "clientEnvironmentFallback": "ARCANOS_JOB_READ_TOKEN",
      "publicJobEligibility": {
        "ask": "eligible",
        "gpt": "eligible only with recognized public GPT-route or custom-bridge provenance",
        "gptAccess": "concealed; use the authenticated GPT Access result operation"
      },
      "cacheControl": "no-store"
    },
    "/jobs/{id}/result": {
      "method": "GET",
      "tsRouteFile": "src/routes/genericJobsRouter.ts",
      "pythonClientMethods": [
        "request_gpt_job_result",
        "request_job_result"
      ],
      "requestFields": [
        "id(path)",
        "jobReadToken(header:x-arcanos-job-read-token)"
      ],
      "requiredHeaders": [
        "x-arcanos-job-read-token"
      ],
      "clientEnvironmentFallback": "ARCANOS_JOB_READ_TOKEN",
      "publicJobEligibility": {
        "ask": "eligible",
        "gpt": "eligible only with recognized public GPT-route or custom-bridge provenance",
        "gptAccess": "concealed; use the authenticated GPT Access result operation"
      },
      "cacheControl": "no-store"
    },
    "/api/vision": {
      "method": "POST",
      "tsRouteFile": "src/routes/api-vision.ts",
      "pythonClientMethods": [
        "request_vision_analysis"
      ],
      "requestFields": [
        "imageBase64",
        "prompt",
        "temperature",
        "model",
        "maxTokens"
      ]
    },
    "/api/transcribe": {
      "method": "POST",
      "tsRouteFile": "src/routes/api-transcribe.ts",
      "pythonClientMethods": [
        "request_transcription"
      ],
      "requestFields": [
        "audioBase64",
        "filename",
        "model",
        "language"
      ]
    },
    "/api/update": {
      "method": "POST",
      "tsRouteFile": "src/routes/api-update.ts",
      "pythonClientMethods": [
        "submit_update_event"
      ],
      "requestFields": [
        "updateType",
        "data"
      ]
    }
  }
}
